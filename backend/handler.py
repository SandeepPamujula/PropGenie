# AWS Lambda entry point stub
def lambda_handler(event, context):
    """
    Handles API Gateway requests and invokes the agent graph.
    """
    return {
        'statusCode': 200,
        'body': 'PropGenie Backend Stub'
    }
